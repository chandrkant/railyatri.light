import stationpiker from 'railyatri/utils/stationpiker';
import { module, test } from 'qunit';

module('Unit | Utility | stationpiker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stationpiker();
  assert.ok(result);
});
