
import { etaTime } from 'railyatri/helpers/eta-time';
import { module, test } from 'qunit';

module('Unit | Helper | eta time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = etaTime([42]);
  assert.ok(result);
});

