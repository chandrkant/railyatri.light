
import { staTime } from 'railyatri/helpers/sta-time';
import { module, test } from 'qunit';

module('Unit | Helper | sta time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = staTime([42]);
  assert.ok(result);
});

