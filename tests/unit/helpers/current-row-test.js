
import { currentRow } from 'railyatri/helpers/current-row';
import { module, test } from 'qunit';

module('Unit | Helper | current row');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentRow([42]);
  assert.ok(result);
});

