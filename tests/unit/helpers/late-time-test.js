
import { lateTime } from 'railyatri/helpers/late-time';
import { module, test } from 'qunit';

module('Unit | Helper | late time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lateTime([42]);
  assert.ok(result);
});

