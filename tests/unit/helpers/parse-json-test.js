
import { parseJson } from 'railyatri/helpers/parse-json';
import { module, test } from 'qunit';

module('Unit | Helper | parse json');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseJson([42]);
  assert.ok(result);
});

