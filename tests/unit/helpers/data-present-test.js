
import { dataPresent } from 'railyatri/helpers/data-present';
import { module, test } from 'qunit';

module('Unit | Helper | data present');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dataPresent([42]);
  assert.ok(result);
});

